generator client {
  provider = "prisma-client-js"
  output   = "../../prisma-client/src/generated"
}

datasource db {
  provider = "postgresql"
  url      = env("BAR_DATABASE_URL")
}

model BarUser {
  id    String @id @default(uuid())
  email String
}
